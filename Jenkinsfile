pipeline {
  agent any
  stages {
    stage('download dependencies') {
      steps {
        echo 'download dependencies'
      }
    }

    stage('Build') {
      steps {
        echo 'Build'
      }
    }
  }
}
